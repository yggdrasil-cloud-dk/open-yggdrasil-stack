---
all:

  # hosts and hostvars and all shared vars
  hosts:
    skypath01:
      ansible_connection: local
      inventory_index: 1
    skypath02:
      ansible_host: 192.168.142.4
      inventory_index: 2

  vars:
    dev: true
    default_gateway_interface: ens33
    openstack_mgmt_ip: "10.0.10.{{ inventory_index }}/24"
    ceph_public_ip: "10.0.50.{{ inventory_index }}/24"
    ceph_cluster_ip: "10.0.60.{{ inventory_index }}/24"

  # groups and group vars
  children:
    deployment_controller:
      hosts:
        skypath01:
    deployment_nodes:
      hosts:
        skypath01:
        skypath02:
