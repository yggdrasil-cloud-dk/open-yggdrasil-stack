## Prerequisites ##

Run:
```
# 1. install git and make
apt install git make

# 2. clone repo
git clone <repo>

# 3. enable bash completion (useful for make)
# by commenting out last couple of lines in ~/.bashrc

```
